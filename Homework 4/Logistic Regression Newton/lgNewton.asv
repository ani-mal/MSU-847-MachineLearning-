load data.txt
load labels.txt

epsilon = 0.1 
maxiter = 100

train = data(1:2000, :)
trainLabels = labels(1:2000)

testLabels = labels(2000:4601)
test = data(2001:4601, :)

[m,n] = size(data)

weights = zeros(1, n+1)

[m,n] = size(train)
train = [ ones(m,1) train ]

sig = sigmoid( weights, train )

function sig = sigmoid( weights, data )
    [m,n] = size(data)
    tdata = data'
    wData = data*weights'
    onevector =
    sig = 1./(ones(1,m) + exp( -(wData) ) )
end 



%psudo code 




